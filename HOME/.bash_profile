# The following lines were added by Docker Desktop to add commands to your PATH.
export PATH="$PATH:/Users/andrewsnelling/.docker/bin"
# End of Docker Desktop section.

#!/usr/bin/env bash

if [ -f "$HOME/.bashrc" ]; then
	source "$HOME/.bashrc"
fi
